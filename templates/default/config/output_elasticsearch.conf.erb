output {
  elasticsearch {
<% if @elasticsearch_ip -%>
    host => "<%= @elasticsearch_ip %>"
    cluster => "<%= @elasticsearch_cluster %>"
<% elsif @elasticsearch_embedded -%>
    embedded => true
<% end -%>
<% if @bind_host -%>
    bind_host => "<%= @bind_host %>"
<% end -%>
<% if @es_index -%>
    index => "<%= @es_index %>"
<% end -%>
  }
}
