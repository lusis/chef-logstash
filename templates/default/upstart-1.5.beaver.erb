description "Logstash beaver"
author "Chef"

start on (filesystem and net-device-up)
stop on runlevel [!2345]

respawn
respawn limit 5 30

setuid <%= @user %>
setgid <%= @group %>

exec <%= @cmd %> >> <%= @log %> 2>&1
